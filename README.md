# Todo

デザイン画像をもとにした Vite + React の Todo アプリです。

```sh
npm install
npm run dev
```

`npm run build` で本番用ファイルを `dist/` に生成し、`npm run preview` で確認できます。

タスク追加、完了切り替え、状態別の絞り込み、残件数表示に対応します。タスクは localStorage に保存されます。スマートフォンでも利用できます。

画面の参照画像: `output/imagegen/todo-top.png`
